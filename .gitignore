/node_modules
**/node_modules

# build output
**/dist
**/build
**/.next
**/out
*.tsbuildinfo

# env — .env.example IS committed
.env
.env.*
!.env.example

# test / coverage
**/coverage
.nyc_output

# logs
*.log
npm-debug.log*
logs/

# editor / os
.DS_Store
Thumbs.db
.idea
.vscode/*
!.vscode/extensions.json

# runtime data
/data

# SDD workspace (git-ignored scratch)
/.superpowers

# Postman desktop app's own local-workspace scratch state — regenerated every
# time the app opens the imported collection, not part of the collection itself.
postman/.postman/
postman/postman/

# Removed from git history at the user's request (2026-09-19) — ignored so
# they can't be accidentally re-tracked. Kept locally, not in the repo.
# plan.md is deliberately NOT in this list — it's tracked and pushed.
/CLAUDE.md
/docker-compose.dev.yml
/backend/.env.example
/docs
